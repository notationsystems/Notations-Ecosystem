# Absolute origin of the control plane for a deployed dock. Leave unset in development:
# `npm run dev` proxies /cp -> http://127.0.0.1:8787 (override with CONTROL_PLANE_URL).
# The dock origin must be listed in CONTROL_PLANE_ALLOWED_ORIGINS on the control plane.
VITE_CONTROL_PLANE_URL=
